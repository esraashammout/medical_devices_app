import 'package:flutter/material.dart';

class Clickforedittext extends StatelessWidget {
  const Clickforedittext({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding:  EdgeInsets.all(4),
      child: Text("اضغط لتعديل الصورة "),
    );
  }
}