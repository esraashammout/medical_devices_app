import 'package:flutter/material.dart';

class Doctornamwtext extends StatelessWidget {
  const Doctornamwtext({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(4),
      child: Text("d.mk"),
    );
  }
}